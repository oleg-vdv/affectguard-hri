<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>actuation</name>
  <version>0.1.0</version>
  <description>
    Actuation layer. sim_backend (Phase 1/3) takes the policy engine's
    BehaviorCommand from core/cmd and forwards movement to the
    simulated robot's /cmd_vel, plus MVP voice/face stand-ins. A
    real-hardware backend with the same topic interface is Phase 5.
  </description>
  <maintainer email="ipgleg@gmail.com">Oleg Vdovin</maintainer>
  <license>MIT</license>

  <depend>rclpy</depend>
  <depend>geometry_msgs</depend>
  <depend>std_msgs</depend>
  <depend>interfaces</depend>

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
